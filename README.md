# auction-api
Sample Golang REST API to create and bid on auctions


Instructions:

1. Run `docker compose up` to start the database
2. Run `go run server.go` to run the migrations and start the server
